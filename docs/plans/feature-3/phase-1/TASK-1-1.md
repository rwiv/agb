# TASK-1-1: 의존성 추가 및 기본 환경 설정

## 목표
- `shellexpand` 라이브러리를 프로젝트에 추가하고 빌드 확인.

## 작업 내용
1. `Cargo.toml`에 `shellexpand` 의존성 추가.
2. `cargo check`를 통한 의존성 설치 및 정상 동작 확인.

## 성공 기준
- `Cargo.lock`이 업데이트되고 프로젝트가 정상적으로 컴파일됨.
