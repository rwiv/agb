# TASK 2-2: `src/transformer/README.md` 및 프로젝트 루트 `README.md` 업데이트

## 1. 개요
프로젝트와 모듈의 주요 문서에서 `transformers` 모듈 이름을 `transformer`로 업데이트합니다.

## 2. 세부 작업 지침
- `src/transformer/README.md`: 
  - 제목(`# Transformers Module`을 `# Transformer Module`로) 및 본문 내의 `transformers` 언급을 `transformer`로 업데이트합니다.
  - 모듈 경로 정보(`src/transformers/`)를 `src/transformer/`로 수정합니다.
- 프로젝트 루트 `README.md`:
  - `src/transformers/` 경로를 `src/transformer/`로 변경합니다.
  - 설명 텍스트 내의 `transformers` 언급을 `transformer`로 업데이트합니다.

## 3. 성공 기준
- `README.md` 파일들 내의 모든 `transformers` 언급이 `transformer`로 성공적으로 업데이트되어야 합니다.
- 수정된 문서가 실제 코드 구조를 정확히 반영해야 합니다.
