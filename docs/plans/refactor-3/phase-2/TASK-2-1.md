# TASK 2-1: `docs/specs/TECH_SPEC.md` 내의 모듈 경로 및 설명 업데이트

## 1. 개요
기술 사양서에서 `transformers` 모듈로 명명된 부분을 단수형인 `transformer`로 업데이트합니다. 이 작업은 시스템 아키텍처 문서와 실제 코드 간의 일관성을 유지하기 위해 필수적입니다.

## 2. 세부 작업 지침
- `docs/specs/TECH_SPEC.md`: 
  - `src/transformers/` 디렉토리 경로를 `src/transformer/`로 수정합니다.
  - 모듈 구조 표에서 `transformers`를 `transformer`로 변경합니다.
  - 설명 텍스트 내의 `transformers` 언급을 `transformer`로 업데이트합니다.

## 3. 성공 기준
- `TECH_SPEC.md`의 모든 기술 설명이 단수형 모듈 이름인 `transformer`를 사용해야 합니다.
- 수정된 문서 내용이 시스템 설계 사상에 부합해야 합니다.
