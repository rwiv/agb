# TASK 5: 시스템 문서 업데이트 (디자인, 명세, 모듈 README)

## 상세 설명
멀티라인 지원이 완료됨에 따라 시스템 문서들에 명시된 "멀티라인 미지원" 관련 제약 사항들을 제거하고 변경된 로직(YAML 기반)을 업데이트합니다.

## 수정 대상 문서 및 상세

1.  **`specs/design.md`**:
    - `3.3 동기화 패치 알고리즘 (MdPatcher)` 섹션에서 멀티라인 처리 시 에러를 반환한다는 내용 삭제.
    - `serde_yaml`을 사용한 프론트매터 업데이트 방식으로 설명 변경.

2.  **`specs/spec.md`**:
    - `5. 동기화 규격 (Sync Specifications)` 섹션의 "단, 멀티라인 description은 지원되지 않으며 감지 시 에러를 반환합니다." 문구 삭제.

3.  **`src/syncer/README.md`**:
    - `2. MdPatcher (patcher.rs)` 섹션의 제한 사항(멀티라인 데이터 감지 시 중단) 내용 제거.
    - 정규표현식 대신 YAML 파서 기반 업데이트를 수행한다는 점 명시.

## 수락 기준
- 모든 문서에서 멀티라인 `description`이 동기화 가능하다는 점이 명확히 기술됨.
- 최신 구현 방식(YAML 파서 기반)이 문서에 반영됨.
